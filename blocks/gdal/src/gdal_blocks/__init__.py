"""Spade block collection wrapping the GDAL/OGR Python bindings.

Block IDs use the ``gdal.<name>`` prefix (per ``SPECIFICATION.md``); the
Python package is named ``gdal_blocks`` to avoid shadowing ``osgeo.gdal``
and the PyPI ``gdal`` distribution.
"""
