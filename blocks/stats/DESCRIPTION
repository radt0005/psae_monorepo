Package: stats.collection
Title: Spade stats block collection
Version: 0.1.0
Description: Dependency manifest for the stats Spade block collection. Lists the
    CRAN packages the handlers use directly; the local spade runtime library is
    installed separately (see the collection README) until it is published.
Imports:
    jsonlite
Encoding: UTF-8
