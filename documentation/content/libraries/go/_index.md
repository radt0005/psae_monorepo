+++
title = "Go"
description = "Build Spade blocks in Go with generics and strong type safety."
weight = 4
sort_by = "weight"
insert_anchor_links = "right"
+++

The Go library uses generics to provide type-safe block development. Handler functions receive an `*Args` struct for accessing inputs and parameters, and return typed outputs.

## Prerequisites

- **Go 1.25 or later**
- The Spade CLI installed ([Installation guide](/getting-started/installation/))

## Installation

```bash
go get github.com/spade-dev/spade
```
