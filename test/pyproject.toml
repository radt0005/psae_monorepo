[project]
name = "spade-integration-tests"
version = "0.1.0"
description = "Integration test suite for the spade pipeline system"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "numpy>=1.26",
    "rasterio>=1.4",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
